import { theme } from '@chakra-ui/core';

const customTheme = {
    ...theme,
    fonts: {
        body: 'Inter',
        heading: 'Space Grotesk',
        mono: 'monospace',
    },
};

export default customTheme;
